package com.aztu.support_erp.catalog.dto;

/** One selectable section. The code is what a ticket stores; the name is what the form shows. */
public record SectionResponse(String code, String name) {}
